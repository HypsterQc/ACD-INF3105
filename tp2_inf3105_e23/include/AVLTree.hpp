#ifndef AVLTREE_HPP
#define AVLTREE_HPP

#include <iostream>

/*

// Structure d'un noeud de l'arbre AVL
struct Node {
    int data;           // Valeur du noeud
    int height;         // Hauteur du noeud
    Node* left;         // Pointeur vers le sous-arbre gauche
    Node* right;        // Pointeur vers le sous-arbre droit

    // Constructeur par défaut
    Node(int val) : data(val), height(1), left(nullptr), right(nullptr) {}
};

// Classe représentant un arbre AVL
class AVLTree {
private:
    Node* root;         // Racine de l'arbre

    // Méthodes privées pour les opérations sur l'arbre
    Node* insert(Node* node, int value);
    Node* remove(Node* node, int value);
    int getHeight(Node* node);
    int getBalance(Node* node);
    Node* rotateRight(Node* node);
    Node* rotateLeft(Node* node);
    Node* findMin(Node* node);
    void printInorder(Node* node);

public:
    // Constructeur par défaut
    AVLTree() : root(nullptr) {}

    // Méthodes publiques pour les opérations sur l'arbre
    void insert(int value);
    void remove(int value);
    void printInorder();
};

*/

#endif